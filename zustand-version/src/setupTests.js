// Vitest setup for zustand-version
// Keep minimal to avoid adding jest-dom or other test matchers.
// Ensure React act environment flag to silence warnings when using react-dom's act.
globalThis.IS_REACT_ACT_ENVIRONMENT = true;

// Add any global mocks or test helpers here.
